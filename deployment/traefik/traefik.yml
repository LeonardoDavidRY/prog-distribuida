api:
  insecure: true
  dashboard: true
entryPoints:
  http:
    address: ":80"
  traefik:
    address: ":8888"
providers:
  # file:
  #   filename: app-static.yml
  #   watch: true
  consulCatalog:
    refreshInterval: 5s
    exposedByDefault: true
    endpoint:
      address: "localhost:8500"