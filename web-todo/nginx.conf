events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    upstream app_todo {
        server todo:8080;
    }

    server {
        listen 80;

        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location /app-todo/ {
            proxy_pass http://app_todo/;
            proxy_set_header Host $host;
        }
    }
}
