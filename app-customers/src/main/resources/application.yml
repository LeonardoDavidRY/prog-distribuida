server:
  port: 8090
spring:
  config:
    import: "optional:consul:"
  application:
    name: app-customers
  datasource:
    url: jdbc:postgresql://localhost:5432/books20262026
    username: postgres
    password: root
  cloud:
    consul:
      host: localhost
      port: 8500

management:
  endpoints:
    web:
      exposure:
        include: health,info
